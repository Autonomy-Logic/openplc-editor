// Minimal <new> for AVR -- placement new
#ifndef _AVR_NEW
#define _AVR_NEW
#include <cstddef>
inline void* operator new(size_t, void* p) noexcept { return p; }
inline void* operator new[](size_t, void* p) noexcept { return p; }
#endif
