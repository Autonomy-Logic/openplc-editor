name: test

on: [push,pull_request]

jobs:
  runs-on: [windows-latest]
  steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v3
      with:
        node-version: '18'
    - run: npm install
    - run: jest

