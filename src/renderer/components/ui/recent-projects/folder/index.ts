/* eslint-disable import/prefer-default-export */
import Label from './folder-label';
import Root from './folder-root';
import Shape from './folder-shape';

export const Folder = {
  Label,
  Root,
  Shape,
};
