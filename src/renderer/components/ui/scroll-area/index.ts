import { CustomDisplay } from './display'
import { Root } from './root'
import { ScrollBar } from './scrollbar'
import { Viewport } from './viewport'

const ScrollAreaComponent = {
	CustomDisplay,
	Root,
	ScrollBar,
	Viewport,
}

export default ScrollAreaComponent
