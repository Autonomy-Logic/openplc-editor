import { FC } from 'react'
/**
 * Functional component representing a Project
 * @component
 */
const Project: FC = () => {
  /**
   * Render the Project component
   * @returns JSX Element
   */
  return <div className="h-full w-full bg-red-500">Project</div>
}

export default Project
