import { FC } from 'react'
/**
 * Functional component representing a Resource (Res)
 * @component
 */
const Res: FC = () => {
  /**
   * Render the Res component
   * @returns JSX Element
   */
  return <div className="h-full w-full bg-yellow-500">Res</div>
}

export default Res
